using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ConfigControls : MonoBehaviour
{
    void Start()
    {

    }

    void Update()
    {
        /*
        foreach (KeyCode vKey in System.Enum.GetValues(typeof(KeyCode)))
        {
            if (Input.GetKey(vKey))
            {
              

            }
        }
        string menssage = $"Xbox: {Input.GetButton("A Button XBOX")} Ps4: {Input.GetButton("X Button PS4")}";
        menssage = $"Xbox: {Input.GetKey(KeyCode.Joystick1Button0)} Ps4: {Input.GetKey(KeyCode.Joystick2Button1)}";
        Debug.Log(menssage);
        */

    }
}
